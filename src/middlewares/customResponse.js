
export const customResponse = async (req, res, next) => {
  // console.log('ccc');
  // const { statusCode, message, error, data } = res.locals.responseData
  // const response = {
  //   status: 'success',
  //   message: message || 'OK',
  //   data: data || {}
  // }

  // if (error) {
  //   response.status = 'error'
  // }

  // return res.status(statusCode).json(response)
}
